import { ExtractStatsPipe } from './extract-stats-pipe';

describe('ExtractStatsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractStatsPipe();
    expect(pipe).toBeTruthy();
  });
});
